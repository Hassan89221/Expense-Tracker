generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Expense {
  id        Int      @id @default(autoincrement())
  amount    Float
  category  String
  note      String?
  date      String
  createdAt String   @map("created_at")

  @@map("expenses")
}
